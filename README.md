# مانا - دستیار هوشمند همه‌کاره 🐱✨

یک اپلیکیشن Flutter زیبا و قدرتمند با تم بنفش-طلایی که از Google Gemini AI استفاده می‌کند.

## ویژگی‌ها

### 🎨 طراحی
- تم بنفش-طلایی جذاب
- انیمیشن‌های روان و زیبا
- پشتیبانی از تم تیره و روشن
- فونت فارسی Vazirmatn

### 💬 چت هوشمند
- چت با Google Gemini AI
- ذخیره تاریخچه پیام‌ها در ابر (Firestore)
- تشخیص خودکار نوع پیام (فال، کپشن، خلاصه)
- انیمیشن‌های زیبا برای پیام‌ها
- یادگیری از رفتار کاربر و شخصی‌سازی پاسخ‌ها

### 🔐 احراز هویت
- ورود با Google
- ورود با Apple (iOS/macOS)
- همگام‌سازی خودکار داده‌ها در ابر
- پشتیبانی از حالت بدون ورود

### 🌅 صبحانه مانا (Morning Mana)
- پیام صبحانه انرژی‌بخش
- آب‌وهوا
- فال حافظ روزانه
- کارهای امروز
- خبر ورزشی
- جمله انگیزشی

### 🌙 شب‌نامه مانا (Night Summary)
- خلاصه روز (کارهای انجام شده)
- پیشنهاد آهنگ آرامش‌بخش
- پیشنهاد فیلم/سریال
- ژورنال شبانه

### 🧠 یادگیری و شخصی‌سازی
- یادگیری از رفتار کاربر
- ذخیره ترجیحات و علایق
- پیشنهادهای شخصی‌سازی‌شده
- تحلیل لحن و سبک کاربر

### 🔮 فال حافظ
- فال تصادفی
- فال بر اساس ماه تولد
- تفسیر شخصی‌سازی‌شده با AI

### 📝 تولید محتوا
- کپشن اینستاگرام
- بیو پروفایل
- هشتگ‌های مناسب
- خلاصه‌سازی متن

### 🎯 آیکون شناور
- آیکون متحرک و قابل drag
- انیمیشن pulse و چرخش
- Double tap برای منوی سریع
- Long press برای تنظیمات

### ⚙️ تنظیمات
- مدیریت پروفایل کاربر
- پاک کردن چت
- خروج و حذف اطلاعات

## نصب و راه‌اندازی

### پیش‌نیازها
- Flutter SDK 3.5.0 یا بالاتر
- Dart SDK
- Android Studio / VS Code
- Google Gemini API Key
- Firebase Project (برای ورود و ذخیره‌سازی ابری)

### مراحل نصب

1. **کلون کردن پروژه**
```bash
git clone <repository-url>
cd Magic_AI
```

2. **نصب وابستگی‌ها**
```bash
flutter pub get
```

3. **تنظیم Firebase**
   - به [Firebase Console](https://console.firebase.google.com/) بروید
   - پروژه جدید ایجاد کنید
   - Android و iOS app اضافه کنید
   - فایل‌های `google-services.json` و `GoogleService-Info.plist` را دانلود کنید
   - راهنمای کامل در `FIREBASE_SETUP.md`

4. **تنظیم API Key**
   - فایل `lib/services/gemini_service.dart` را باز کنید
   - در خط 11، `YOUR_GEMINI_API_KEY` را با کلید واقعی Gemini جایگزین کنید
   - یا از فایل `.env` استفاده کنید (نیاز به پکیج `flutter_dotenv`)

5. **اجرای اپلیکیشن**
```bash
flutter run
```

## ساختار پروژه

```
lib/
├── core/
│   └── theme/
│       └── app_theme.dart          # تم بنفش-طلایی
├── models/
│   ├── user_profile.dart             # مدل پروفایل کاربر
│   └── chat_message.dart             # مدل پیام چت
├── providers/
│   ├── user_provider.dart            # مدیریت پروفایل
│   └── chat_provider.dart            # مدیریت چت
├── screens/
│   ├── onboarding_screen.dart        # صفحه آنبوردینگ
│   ├── home_screen.dart              # صفحه اصلی (چت)
│   └── settings_screen.dart          # تنظیمات
├── services/
│   ├── gemini_service.dart           # سرویس Gemini AI
│   └── hafez_service.dart           # سرویس فال حافظ
├── widgets/
│   ├── floating_icon.dart            # آیکون شناور متحرک
│   └── typing_indicator.dart        # نشانگر تایپ
├── utils/
│   └── constants.dart                # ثابت‌ها
└── main.dart                         # نقطه ورود اپلیکیشن
```

## وابستگی‌ها

### UI & Animations
- `flutter_animate`: انیمیشن‌های زیبا
- `google_fonts`: فونت‌های Google
- `lottie`: انیمیشن‌های Lottie
- `rive`: انیمیشن‌های Rive

### State Management
- `provider`: مدیریت state

### Storage
- `shared_preferences`: ذخیره‌سازی ساده

### Network & API
- `google_generative_ai`: اتصال به Gemini AI
- `http`: درخواست‌های HTTP
- `dio`: HTTP client پیشرفته

### Firebase
- `firebase_core`: هسته Firebase
- `firebase_auth`: احراز هویت
- `cloud_firestore`: پایگاه داده ابری
- `firebase_storage`: ذخیره‌سازی فایل
- `google_sign_in`: ورود با Google
- `sign_in_with_apple`: ورود با Apple

### Utilities
- `intl`: بین‌المللی‌سازی
- `shamsi_date`: تاریخ شمسی
- `persian_tools`: ابزارهای فارسی

## استفاده

### گرفتن فال حافظ
1. روی آیکون شناور دوبار ضربه بزنید
2. "فال حافظ" را انتخاب کنید
3. یا مستقیماً بنویسید: "فال حافظ بگیر"

### تولید کپشن
1. بنویسید: "یه کپشن برای اینستاگرام بنویس"
2. موضوع را مشخص کنید
3. مانا کپشن مناسب را تولید می‌کند

### خلاصه‌سازی متن
1. بنویسید: "این متن رو خلاصه کن:"
2. متن را وارد کنید
3. خلاصه دریافت کنید

## نکات مهم

⚠️ **API Key**: حتماً API Key واقعی Gemini را تنظیم کنید، در غیر این صورت اپلیکیشن کار نمی‌کند.

📱 **پلتفرم**: این پروژه برای Android و iOS طراحی شده است.

🎨 **تم**: تم پیش‌فرض تیره است، می‌توانید در `main.dart` تغییر دهید.

## توسعه

### افزودن ویژگی جدید
1. مدل داده را در `lib/models/` ایجاد کنید
2. Provider را در `lib/providers/` اضافه کنید
3. صفحه UI را در `lib/screens/` بسازید
4. سرویس را در `lib/services/` پیاده‌سازی کنید

### بهبود UI
- رنگ‌ها در `lib/core/theme/app_theme.dart`
- انیمیشن‌ها با `flutter_animate`
- ویجت‌های سفارشی در `lib/widgets/`

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## مشارکت

مشارکت‌ها خوش‌آمدند! لطفاً:
1. Fork کنید
2. Branch جدید بسازید
3. تغییرات را commit کنید
4. Pull Request بفرستید

## پشتیبانی

اگر مشکلی دارید یا سوالی دارید، لطفاً Issue ایجاد کنید.

---

ساخته شده با ❤️ و Flutter
